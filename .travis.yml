language: node_js
# Test on a selection of operating systems
os:
  - linux
  - osx
  - windows
# Build with both the latest and latest long-term-support by default
node_js:
  - node
  - lts/*
# Build by running the tests (this is the Travis default)
script: npm test
